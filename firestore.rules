rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && request.auth.token.role == 'admin';
    }

    function isTeacher() {
      return isSignedIn() && request.auth.token.role == 'teacher';
    }

    function isStudent() {
      return isSignedIn() && request.auth.token.role == 'student';
    }

    function isResourceOwner() {
      return isSignedIn() && resource.data.teacherId == request.auth.uid;
    }

    function isCreatingOwnResource() {
      return isSignedIn() && request.resource.data.teacherId == request.auth.uid;
    }

    match /{document=**} {
      allow read, write: if false;
    }

    match /users/{userId} {
      allow read, update: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null && request.auth.token.role == 'admin';
    }
    
    match /student_data/{studentId} {
        allow read: if request.auth != null && (request.auth.uid == studentId || request.auth.token.role in ['teacher', 'admin']);
        allow update: if request.auth != null && request.auth.uid == studentId;
        allow create, delete: if request.auth != null && request.auth.token.role == 'admin';
    }

    match /teachers/{teacherId} {
        allow read: if request.auth != null;
        allow update: if request.auth != null && (request.auth.uid == teacherId || request.auth.token.role == 'admin');
        allow create, delete: if request.auth != null && request.auth.token.role == 'admin';
    }

    match /courses/{courseId} {
      allow read: if request.auth != null;
      allow create, update, delete: if request.auth != null && request.auth.token.role in ['teacher', 'admin'];
    }

    match /course-student-list/{courseKey} {
        allow read, write: if request.auth != null && request.auth.token.role in ['teacher', 'admin'];
        allow read: if request.auth != null && request.auth.token.role == 'student';
    }

    // 點名活動
    match /courses/{courseId}/attendance/{activityId} {
      // 老師和管理員可以建立、讀取、更新、刪除點名活動
      allow write: if request.auth != null && (request.auth.token.role in ['teacher', 'admin']);
      
      // 學生只能讀取活動
      allow read: if request.auth != null && request.auth.token.role == 'student';

        // 點名參與者 (學生簽到)
        match /attendees/{studentId} {
          // 學生只能為自己建立簽到紀錄
          allow create: if request.auth != null && request.auth.uid == studentId;
          // 老師和管理員可以讀取簽到紀錄
          allow read: if request.auth != null && (request.auth.token.role in ['teacher', 'admin'] || request.auth.uid == studentId);
        }
    }

    // 請假申請 (修正：移出 attendance 層級，獨立在課程下)
    match /courses/{courseId}/leave_requests/{studentId} {
        // 老師和管理員可以讀寫所有申請
        allow read, write: if request.auth != null && (request.auth.token.role in ['teacher', 'admin']);
        // 學生可以建立、讀取、更新自己的申請
        allow create, read, update: if request.auth != null && request.auth.uid == studentId;
    }

    match /announcements/{announcementId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null && request.auth.token.role == 'admin';
    }

    match /exam_dates/{examId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null && request.auth.token.role == 'admin';
    }

    match /time-slots/{slotId} {
      allow read: if request.auth != null;
      // 只有後端 (Admin SDK) 可以寫入
      allow write: if false;
    }

    match /resources/{resourceId} {

      allow read: if isAdmin() || isStudent() || (isTeacher() && resource.data.teacherId == request.auth.uid);
      

      allow create: if isAdmin() || (isTeacher() && isCreatingOwnResource());
      allow update, delete: if isAdmin() || (isTeacher() && isResourceOwner());
    }
合
    match /courses/{courseId}/{collection}/{documentId} {
       allow read, write: if request.auth != null && request.auth.token.role in ['teacher', 'admin'];
       allow read: if request.auth != null && request.auth.token.role == 'student' && resource.data.studentId == request.auth.uid;
    }
  }
}